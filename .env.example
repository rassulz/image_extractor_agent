# Optional. Without this key the notebook uses TF-IDF and placeholder descriptions.
OPENAI_API_KEY=

# Models can be changed without editing Python files.
OPENAI_VISION_MODEL=gpt-4.1-mini
OPENAI_EMBEDDING_MODEL=text-embedding-3-small
